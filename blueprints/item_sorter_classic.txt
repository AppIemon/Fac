# 설계도: 호퍼 아이템 분류기 (item_sorter_classic)
기준 버전: Minecraft 26.2 (Chaos Cubed, 2026-06-16)
분류: 인프라/공정 / 차원: overworld / 난이도: ★★★
영문명: Item Sorter
아키타입: item_sorter
검증 상태: mechanics
참고 설계 계보(검색 키워드): ilmango, Mumbo Jumbo
AFK 필요: 예

========================================================================
1. 작동 원리
========================================================================
비교기가 분류 호퍼의 필터 아이템 신호를 읽음 → 일치할 때만 호퍼 잠금 해제 → 지정 상자로

========================================================================
2. 규격
========================================================================
  가로(X) 23 x 높이(Y) 3 x 세로(Z) 3 블록
  배치 블록 수: 84개
  예상 산출: 채널 12개, 라인 처리량 초당 2.5개 (병렬 시 x라인수)

========================================================================
3. 층별 평면도 (위에서 내려다본 뷰)
========================================================================
── Y=+0 층 ───────────────────
    01234567890123456789012
  1 #.#.#.#.#.#.#.#.#.#.#.#
  0 c.c.c.c.c.c.c.c.c.c.c.c
  1 #.#.#.#.#.#.#.#.#.#.#.#

── Y=+1 층 ───────────────────
    01234567890123456789012
  1 r.r.r.r.r.r.r.r.r.r.r.r
  0 h.h.h.h.h.h.h.h.h.h.h.h
  1 ?.?.?.?.?.?.?.?.?.?.?.?

── Y=+2 층 ───────────────────
    01234567890123456789012
  1 .......................
  0 h.h.h.h.h.h.h.h.h.h.h.h
  1 .......................

기호  블록
 #    건축 블록(아무 불투명 블록)
 ?    설계 미지정(직접 결정)
 c    상자
 h    호퍼
 r    레드스톤 가루

========================================================================
4. 재료 목록
========================================================================
  호퍼                               24개
  건축 블록(아무 불투명 블록)                 24개
  상자                               12개
  레드스톤 가루                          12개

========================================================================
5. 시공 순서
========================================================================
  1) 상단에 좌→우로 흐르는 이송 호퍼 라인을 깐다.
  2) 각 채널마다 이송 라인 아래로 '위를 향한 분류 호퍼'를 붙인다.
  3) 분류 호퍼 5칸 중 1칸에 필터 아이템 1개, 나머지 4칸에 아무 아이템 1개씩(스택 불가 물품 권장).
  4) 비교기로 신호를 뽑아 레드스톤 블록으로 호퍼 잠금(hopper lock)을 건다.
  5) 분류 호퍼 아래에 목적지 상자를 둔다.
  6) 라인 끝에 오버플로 상자를 두어 미분류 아이템을 받는다.

========================================================================
6. 설계 메모
========================================================================
  * 분류 호퍼에 필터 아이템 1종 x 18개 + 이름표 붙인 아이템 4칸 = 표준 필터

========================================================================
7. 경고 / 자주 실패하는 지점
========================================================================
  ! 필터 슬롯 구성이 틀리면 엉뚱한 아이템이 새어 나간다. 필터 아이템 18개 + 채움용 4개가 표준.
  ! 라인 처리량은 호퍼 1개 = 초당 2.5개. 고속 팜은 라인을 병렬로 나눌 것.

========================================================================
8. 시공 후 점검표
========================================================================
  [ ] 스폰 구역 블록광이 0인가? (횃불/용암/빛나는 블록 전부 제거)
  [ ] 주변 128블록 내 경쟁 스폰 지면을 차단했는가? (동굴 밀봉, 반 블록/광원)
  [ ] AFK 지점이 스폰 구역에서 24블록 이상, 수거부에서 128블록 이내인가?
  [ ] 아이템이 5분 안에 호퍼에 들어가는가? (안 그러면 소멸)
  [ ] 호퍼 처리량이 산출량보다 큰가? (호퍼 1개 = 초당 2.5개)
  [ ] 청크 로딩 대책이 있는가? 스폰 청크는 1.21.9에서 삭제됨 → AFK 또는 /forceload
  [ ] 몹이 물길 밖으로 새어나가는 틈이 없는가? (거미는 벽 등반, 엔더맨은 순간이동)
  [ ] 상자가 가득 찼을 때 팜이 멈추지 않게 오버플로 라인이 있는가?
